# Core Gradio and web dependencies (Essential)
gradio>=4.0.0
aiohttp>=3.9.0
aiofiles>=23.0.0

# Basic data handling (Essential for UI)
pandas>=2.0.0  # Needed for displaying results in tables
python-dotenv>=1.0.0  # For environment variables

# Date utilities (Essential)
python-dateutil>=2.8.0

# Scheduling for cleanup (Optional)
schedule>=1.2.0
